node_modules/
node_modules/*
docs/.vitepress/cache/
docs/.vitepress/dist/

# HyperFrames video project (source tracked; deps & render output ignored)
rust-memory/node_modules/
rust-memory/renders/
rust-memory/package-lock.json

# Rust build artifacts
/target/
**/target/
rustc*/
/build/
*.exe
*.o
*.pdb
*.dSYM/

# `rustc *.rs` → same-basename executable at repo root (no extension).
# Ignore all root files, then un-ignore tracked sources and directories.
/*
!/*/
!/*.rs
!/*.md
!/*.json
!/*.yaml
!/.gitignore
