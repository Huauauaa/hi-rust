# Getting Started

This project uses pnpm and VitePress to build the documentation site.

## Development

Install dependencies:

```sh
pnpm install
```

Start the local development server:

```sh
pnpm docs:dev
```

Build the static site:

```sh
pnpm docs:build
```

Preview the production build:

```sh
pnpm docs:preview
```
