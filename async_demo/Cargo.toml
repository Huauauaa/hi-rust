[package]
name = "async_demo"
version = "0.1.0"
edition = "2021"
description = "hi-rust async/await demo crate"

[dependencies]
tokio = { version = "1", features = ["rt", "rt-multi-thread", "macros", "time"] }
